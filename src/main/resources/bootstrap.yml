spring:
  application:
    name: cnheri-token-config
  boot:
    admin:
      client:
        url: http://localhost:8084
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 0.1
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/cnheri-token/cnheri-token-config
          search-paths: respo
          username: richardle1983
          password: Ljb@314159265

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8861/eureka/,http://localhost:8961/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS